#include<stdio.h>
void aos(int s)
{
   int area;
   area = s*s;
   printf("Area of Square : %d", area);
}
void main()
{
   int side;

   printf("\nEnter the Length of Side : ");
   scanf("%d", &side);

   aos(side);
}
